server:
  port: 9040

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/razorpay-vault-db

vault:
  master-key: WTtGqMrItO8DwVD48GUO5zfwU2yc7f891/MdhSWBs7c=

resilience4j:
  thread-pool-bulkhead:
    instances:
      vault-card-processor:
        core-thread-pool-size: 5
        max-thread-pool-size: 10
        queue-capacity: 20


---
spring:
  config:
    activate:
      on-profile: k8s
  datasource:
    url: jdbc:postgresql://postgres.razorpay-core.svc.cluster.local:5434/razorpay-vault-db
    username: vault_user
    password: ${VAULT_DB_PASSWORD}

vault:
  master-key: ${VAULT_MASTER_KEY}
